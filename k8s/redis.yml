apiVersion: apps/v1
kind: Deployment
metadata:
  name: dana-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dana-redis
  template:
    metadata:
      labels:
        app: dana-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: dana-redis
spec:
  selector:
    app: dana-redis
  ports:
    - port: 6379
      targetPort: 6379